import 'package:flutter/material.dart';

class CustomColors {
  static const textColor = Color(0xff000000);
  static const textButtonColor = Color(0xfff393a58);
  static const loginButtonTextColor = Color(0xffffffff);
}